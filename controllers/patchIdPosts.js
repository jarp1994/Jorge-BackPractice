

const patchIdPosts = async (req, res) => {
    try {
    
        
    } catch (error) {
        res.status(400).send({error: "Error al hacer el patch con los id"})
    }
}

module.exports = patchIdPosts